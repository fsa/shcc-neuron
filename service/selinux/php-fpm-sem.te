
module php-fpm-sem 1.0;

require {
	type unconfined_service_t;
	type httpd_t;
	class sem { associate read unix_read unix_write write };
}

#============= httpd_t ==============
allow httpd_t unconfined_service_t:sem { associate read unix_read unix_write write };
